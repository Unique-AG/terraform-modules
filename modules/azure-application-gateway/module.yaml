name: azure-application-gateway
sources:
  - https://github.com/Unique-AG/terraform-modules/tree/main/modules/azure-application-gateway
version: 4.6.0
compatibility:
  unique.ai: ~> 2025.32
changes:
  - kind: added
    description: "WAF can now block custom headers and by default blocks the header x-service-id."
