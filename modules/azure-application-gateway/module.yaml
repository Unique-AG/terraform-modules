name: azure-application-gateway
sources:
  - https://github.com/Unique-AG/terraform-modules/tree/main/modules/azure-application-gateway
version: 3.0.1
changes:
  - kind: fixed
    description: "Fixes the waf_ip_allow_list to accept a list of IP addresses or ranges as having multiple ip lists does not work."
