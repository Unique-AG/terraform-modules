name: azure-speech-service
sources:
  - https://github.com/Unique-AG/terraform-modules/tree/main/modules/azure-speech-service
version:  1.0.2
changes:
  - kind: fixed
    description: "The public_network_access_enabled parameter is now optional and defaults to true to avoid breaking changes"