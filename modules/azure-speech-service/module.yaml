name: azure-speech-service
sources:
  - https://github.com/Unique-AG/terraform-modules/tree/main/modules/azure-speech-service
version: 4.3.0
compatibility:
  unique.ai: ~> 2025.26
changes:
  - kind: added
    description: "Support for log category groups (`enabled_log_category_groups`) on diagnostic settings, alongside existing `enabled_log_categories`."
