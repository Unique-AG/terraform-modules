name: azure-storage-account
sources:
  - https://github.com/Unique-AG/terraform-modules/tree/main/modules/azure-storage-account
version: 4.0.1
compatibility:
  unique.ai: ~> 2025.24
changes:
  - kind: fixed
    description: "Azure automatically adjusts retention settings when backup is enabled for blob storage to ensure reliable backup and restore operations. This behavior is now implemented in the module to prevent drift but could be configured should Azure adjust their behavior."
