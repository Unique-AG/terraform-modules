name: azure-document-intelligence
sources:
  - https://github.com/Unique-AG/terraform-modules/tree/main/modules/azure-document-intelligence
version: 3.4.0
compatibility:
  unique.ai: ~> 2025.16
changes:
  - kind: added
    description: "Diagnostic settings support (`diagnostic_settings` per-account and global) including `log_categories`, `log_category_groups`, and `metric_categories`."
