name: azure-postgresql
sources:
  - https://github.com/Unique-AG/terraform-modules/tree/main/modules/azure-postgresql
version: 3.5.0
compatibility:
  unique.ai: ~> 2025.16
changes:
  - kind: added
    description: "Add metrics.collector_database_activity=ON (enhanced metrics) and pgms_wait_sampling.query_capture_mode=ALL (wait sampling) to the default parameter_values to improve database debugging. These are nice-to-have and can be omitted by callers that override parameter_values."
