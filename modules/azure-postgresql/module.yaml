name: azure-postgresql
sources:
  - https://github.com/Unique-AG/terraform-modules/tree/main/modules/azure-postgresql
version: 3.5.0
compatibility:
  unique.ai: ~> 2025.16
changes:
  - kind: added
    description: "Always apply metrics.collector_database_activity=ON (enhanced metrics) and pgms_wait_sampling.query_capture_mode=ALL (wait sampling) to improve database debugging. Both remain overridable via parameter_values."
