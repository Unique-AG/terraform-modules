name: azure-log-analytics-data-collection-rule
sources:
  - https://github.com/Unique-AG/terraform-modules/tree/main/modules/azure-log-analytics-data-collection-rule
version: 2.0.0
changes:
  - kind: removed
    description: "Query-string redaction helper input and derived DCR naming inputs."
  - kind: changed
    description: "Module interface now requires a DCR `name`, keeps destination naming as an explicit override, and uses `transformations` as the main behavior input."
