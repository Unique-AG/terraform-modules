name: "Release Azure Log Analytics Data Collection Rule"

on:
  workflow_dispatch: # allow manual runs for missed release workflow updates
  push:
    branches:
      - main
    paths:
      - modules/azure-log-analytics-data-collection-rule/module.yaml
  pull_request:
    branches:
      - main
    paths:
      - modules/azure-log-analytics-data-collection-rule/**

jobs:
  validate:
    if: github.event_name == 'pull_request'
    uses: ./.github/workflows/immutable-artifact.yaml
    with:
      module: azure-log-analytics-data-collection-rule

  release:
    if: ${{ github.event.ref == 'refs/heads/main' }}
    uses: ./.github/workflows/release.yaml
    with:
      module: azure-log-analytics-data-collection-rule
